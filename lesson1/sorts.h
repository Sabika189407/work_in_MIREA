#include <iostream>
#include <string>
#include <cstring>
#include <cstdlib>
#include <ctime>
#include <cmath>
#include <chrono>

#pragma once

void bubbleSort_stupid(int*, int);
void insertion_sort(int*, int);
void selectionSort(int*, int);
void merge_sort(int*, int, int);
void quick_sort(int*, int);
void radix_sort(int*, int);
void digit_sort(int*, int, int);
int Stalin_sort(int*, int);
void cocktail_sort(int*, int);
void heap_sort(int*, int);